package com.loan.aggregator.util;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonElement;
import com.google.gson.JsonParser;

public class JSONUtility {
	
	/*
	 * public static <T> String getJsonObject(T object) { Gson gson = new
	 * GsonBuilder().setPrettyPrinting().create(); JsonElement je =
	 * JsonParser.parseString​(object); String prettyJsonString = gson.toJson(je);
	 * return prettyJsonString; }
	 */

}
